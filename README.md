# What's the Plan — Frontend

React (Vite) client for the What's the Plan app.

## Setup

```bash
npm install
cp .env.example .env   # set VITE_API_URL if backend isn't on localhost:4000
npm run dev
```

Renders the backend's health check status and a Socket.io ping/pong test on load.
