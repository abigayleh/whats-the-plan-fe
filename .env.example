VITE_API_URL="http://localhost:4000"

# Sentry — leave unset locally: with no DSN the SDK never starts.
VITE_SENTRY_DSN=""
# Build-time only (CI): set to upload source maps for the release. Never commit it.
# SENTRY_AUTH_TOKEN=""
