// Mirrors $breakpoint-tablet in styles/abstracts/_variables.scss. Anything narrower is a
// phone, where the app shows one pane at a time instead of a sidebar plus detail.
export const MOBILE_QUERY = '(max-width: 767.98px)';